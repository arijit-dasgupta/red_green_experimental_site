{
  "name": "redgreen-heroku-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "install": "cd frontend && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
